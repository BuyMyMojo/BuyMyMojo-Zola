# All available Hugo versions are listed here: https://gitlab.com/pages/hugo/container_registry
image: registry.gitlab.com/pages/hugo:latest

variables:
  GIT_SUBMODULE_STRATEGY: recursive

test:
  script:
  - hugo
  except:
  - master

pages:
  script:
  - sed -i.bak '1 s/.*/baseurl = "https://buymymojo.gitlab.io/buymymojo-hugo/"/' config.toml
  - cat config.toml
  - hugo
  artifacts:
    paths:
    - public
  only:
  - master
